package ing.aether.data;

import java.util.Map;
import java.util.List;
import ing.aether.Portal;
import ing.aether.SessionTracker;

/**
 * Manages the initialization and lifecycle of FileProviders.
 */
public class ProviderManager 
{
  @SuppressWarnings("unchecked")
  public static void initialize() 
  {
    // User-specific providers are now handled dynamically in SessionTracker
  }
}
